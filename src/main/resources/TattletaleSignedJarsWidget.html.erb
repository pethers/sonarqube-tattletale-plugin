<% if measure('html_signedjars') && measure('signedjars') && measure('totaljars') %>
<div class="yui-g">
  <div class="dashbox" style="width: 100%">
    <p class="title">
      <%= message('widget.signedjars.title') -%>
    </p>
    <p>
      <span class="big"> <%= format_measure('signedjars') -%> <%= tendency_icon(measure('signedjars')) -%></span> /<%= format_measure('totaljars') -%>
    </p>
    <% if (measure('signedjars').value.to_i > 0) %>
      <link rel="stylesheet" type="text/css" href="<%= url_for_static(:plugin => 'sonarTattletalePlugin', :path => 'tattletale-style.css') -%>" />
      <div style="width: 100%; padding-top: 15px; overflow-y: scroll; height: 150px">
        <%= measure('html_signedjars').data -%>
      </div>
    <% end %>
  </div>
</div>
<% end %> 