<div class="yui-g">
	<div class="dashbox" style="height:100%">
		<p class="title">
			<%= message('widget.unusedjars.title') -%>
		</p>
		<p>
			<span class="big"> <%= format_measure('unusedjars') -%> <%= tendency_icon(measure('unusedjars')) -%> </span>
		</p>
		<% if !measure('html_unusedjars') && !measure('unusedjars') %>
			<br />
			<p>
				<%= message('widget.sonar-tattletale-plugin.error') -%>
			</p>
		<% end %>
		<script type="text/javascript">
		
			<% if measure('html_unusedjars') && measure('unusedjars') %>
				function fillsFrames() {
					var htmlData = '<%= measure('html_unusedjars').data -%>';
					document.getElementById('unusedJarsIframe').contentDocument.body.innerHTML = htmlData;
				}

				Event.observe(window, 'load', function() {
					fillsFrames();
				});
			<% end %>
		</script>
	</div>
</div>
<% if measure('html_unusedjars') && measure('unusedjars') %> 
	<iframe id="unusedJarsIframe" width="100%" style="border: 0"></iframe>
<% end %> 